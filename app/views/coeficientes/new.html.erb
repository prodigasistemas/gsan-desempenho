<div class="page-header">
  <%= build_breadcrumb [
    { title: "Contratos", link: contrato_medicoes_path },
    { title: "#{@contrato_medicao.numero} - #{@contrato_medicao.empresa.nome}", link: contrato_medicao_path(@contrato_medicao.id) },
    { title: "Novo Coeficiente", active: true }
  ] %>

  <h1>Coeficiente</h1>
</div>

<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Formulário de Coeficientes</h3>
  </div>

  <%= form_tag contrato_medicao_coeficientes_path(@contrato_medicao.id), class: 'form-horizontal' do %>
    <%= render "form", edit: false %>
  <% end %>
</div>
